@using ProjectTask.Models
@{
    ViewBag.Title = "Home Page";
}

<h2>List</h2>

<div style=" width:90%; margin:0 auto">

    <table id="myTable" class=" table table-striped table table-bordered table table-hover">
        <thead>
            <tr>

                <th>
                    ProductId
                </th>

                <th>ProductName</th>
                <th>CategoryId</th>
                <th>CategoryName</th>

            </tr>




        </thead>



    </table>
</div>



@section scripts
{
    <script>
        $(document).ready(function () {
            $("$Categories").DataTable({
                ajax: {
                    url: "/Controllers/Api/Categories",
                    dataSrc:""
                },
                columns: [
                    {
                        data: "CategoryName",
                        render: function (data, type, Category) {
                            return"<a href="">

                        }
                    }

                ]
            });

            $("#myTable").on("click","js-delete", function ()
            {
                var button = $(this);
                bootbox.confirm("Are you sure you want to delete this Category?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/Controllers/Api/Categories" + button.attr("data-Categories-id"),
                            method: "DELETE",
                            success: function () {
                                button.parents("tr").remove();
                            }
                        });

                    }
                });

            });
        });
    </script>
}


